# Image originals live outside the repo ? the site pulls images from Cloudinary at runtime.
images/

# Local editor / agent settings
.claude/

# OS cruft
Thumbs.db
.DS_Store

# Node deps (installed by Netlify at build time)
node_modules/

# Astro build output + generated types
dist/
.astro/
